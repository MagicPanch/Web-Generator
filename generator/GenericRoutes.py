from flask import request
# Definición de la lógica para las funciones asociadas a las rutas de las APIs

def get_product():
    #return await self.db.get_product(request.json.get('producto'))
    return "RUTA GET PRODUCT"
